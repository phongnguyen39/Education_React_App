import React from 'react';
import './Numbers.css';

const numbers = (props)=> {
    return(
        <div id = 'numbers'>
            <h1>Some number</h1>
        </div>
    );
};

export default numbers;